{
  "name": "pichau-price-tracker",
  "version": "1.0.0",
  "description": "Price tracker for pichau products",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "prompt-sync": "^4.2.0",
    "puppeteer": "^20.2.1"
  },
  "devDependencies": {
    "@types/prompt-sync": "^4.2.0"
  }
}
